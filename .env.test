# Test Environment Configuration
NODE_ENV=test
APP_NAME=Isatis_Test
APP_ENV=testing
APP_DEBUG=true
APP_TIMEZONE=UTC
APP_URL=http://localhost:4001

# API Configuration
GLOBAL_PREFIX=api
PORT=4001

# JWT Configuration
JWT_SECRET=test_jwt_secret_key_very_secret_for_tests
REFRESH_TOKEN_TTL=600000 # 10 minutes
ACCESS_TOKEN_TTL=59999 # ~1 minute
COOKIE_SECRET=test_cookie_secret_for_testing

# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=test_db
POSTGRES_USER=develop
POSTGRES_PASSWORD=123456
POSTGRES_SYNC=true
DB_LOGGING=false

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=null

# MinIO Configuration (for file storage tests)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=test-bucket
MINIO_PUBLIC_ENDPOINT=http://localhost:9000

# Logging
LOG_LEVEL=debug

# CASL Configuration
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=admin123

# Testing Configuration
TEST_TRANSACTION_ISOLATION=true
TEST_SEED_DATA=true

# Next.js Configuration (if used in the project)
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=test_key_123
NEXT_PRIVATE_DEBUG_CACHE=1
NEXT_ANALYZE=false