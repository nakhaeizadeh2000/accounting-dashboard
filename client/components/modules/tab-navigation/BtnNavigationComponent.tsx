import { btnNavigation } from '@/shared/types/btn-navigation.model';
import React from 'react';

type Props = {
  btn?: btnNavigation[];
};

const BtnNavigationComponent = (props: Props) => {
  return <div>BtnNavigationComponent</div>;
};

export default BtnNavigationComponent;
