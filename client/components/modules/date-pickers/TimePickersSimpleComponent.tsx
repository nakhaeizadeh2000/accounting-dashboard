import React from 'react';

type Props = {
  options: {
    label: string;
    getValue: (item: any) => void;
  };
};

const TimePickersSimpleComponent = (props: Props) => {
  return <div>TimePickersSimpleComponent</div>;
};

export default TimePickersSimpleComponent;
